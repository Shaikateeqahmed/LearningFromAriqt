import { Directive } from '@angular/core';

@Directive({
  selector: '[libUiCardHeader], lib-ui-card-header', // Match attribute and element
})
export class CardHeaderDirective {
  constructor() {
    debugger
  }
}
